import styled from "styled-components";

const Dashboard = () => {
  return (
    <div>Dashboard</div>
  );
};

export default Dashboard;

const StyledDashboard = styled.div`
  display: flex;
  flex-direction: row;
  width: 100vw;
  color: red;
`;
